//See https://www.codewars.com/kata/5a905c2157c562994900009d/solutions/cpp
#include <vector>

  std::vector<int> tmp=std::vector<int>(arr.begin(), arr.begin()+i);
  std::vector<int> tmp2=std::vector<int>(arr.begin()+i+1, arr.end());
  tmp.insert(tmp.end(),tmp2.begin(),tmp2.end());
